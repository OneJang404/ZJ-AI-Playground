# ============================================================
# 投标文件智能审核工具 - Python 依赖清单
# ============================================================
# 安装方式：pip install -r requirements.txt
#
# 兼容平台：
#   - Windows 10/11（本地开发）
#   - Linux（飞桨 AI Studio 云端部署）
#
# Python 版本建议：
#   - 首选 Python 3.10（最稳定，所有包均有预编译 wheel）
#   - 支持 Python 3.11 / 3.12
#   - Python 3.13+ 部分包可能缺少预编译 wheel
# ============================================================

# PDF 解析、页面渲染为图片（>=1.25.0 支持 Python 3.10-3.14 预编译 wheel）
pymupdf>=1.25.0,<2.0.0

# 飞桨深度学习框架（CPU 版本）
# 注：如需 GPU 版本，改为 paddlepaddle-gpu
paddlepaddle>=3.0.0,<4.0.0

# PaddleOCR 文字识别引擎（3.x 使用全新 API）
paddleocr>=3.0.0,<4.0.0

# 网页服务框架
streamlit>=1.35.0

# HTTP 网络请求（调用 DeepSeek API）
requests>=2.32.0

# 读取 .env 环境变量配置文件
python-dotenv>=1.0.0

# 图像处理（PIL/Pillow）
Pillow>=10.0.0

# 数值计算库（OCR 图像数据处理）
numpy>=1.21.0,<2.0.0

# Word 文档解析（DOCX/DOC 文本提取）
python-docx>=1.0.0

